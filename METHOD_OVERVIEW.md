# 基于 Summarize 的难题强化学习方法

## 一、研究动机

在数学推理的 RL 训练中，模型对**难题**往往整组 rollout 全错，GRPO 因组内无正样本、优势为零而学不到东西——难题被白白浪费。我们的核心思路是：**当模型自己解不出来时，借助一个"提示更充分的改写版问题"（summarize prompt）引导它生成正确解答，再把这条正确解答以恰当的方式喂回原始问题的训练信号中**，从而让难题重新产生有效梯度。

## 二、方法概述

我们在标准 GRPO 之上引入 **Summarize 引导的 off-policy 注入**，包含两条互补的机制：

- **在线替换（Summarize Replacement）**：训练主循环中，为每道题构造正确的改写解答，替换一条原始采样；
- **失败回收（Failure Recycling）**：将阶段性解不好的难题收集起来，集中用 summarize 提示做渐进式再训练。

两者都通过一种 **prompt-shift 的重要性校正**（explain-style off-policy loss），保证"用长提示生成、按原始短提示学习"在数学上是一致的策略梯度。

## 三、整体流程

**① 常规采样**
每道题在原始问题下采样 8 条推理轨迹，构成一个 GRPO 组。

**② 在线 Summarize 替换**
对每道题，利用其预构造的一组 summarize 提示（信息更完整的问题改写），让当前模型生成候选解答。从候选中筛选出**同时满足"答案正确"且"格式合规"**的一条，将其改写为"原始问题 + 该解答"的形式，替换掉该题的最后一条采样轨迹。若无合格候选，则该题保持不变。

- **候选规模**：每题使用 **4 条** summarize 提示（prefix ratio 为0.6,0.6,0.8,0.8）生成 4 条候选，从中择优选一条合格候选。
- **替换比例**：每题 8 条采样中**替换 1 条（1/8）**，其余 7 条保留为原始 on-policy 轨迹，保证组内在线信号仍占主导。

> 直觉：给模型一个更好懂的问法，它更可能答对；把这条答对的推理回收进原始问题的训练组，为原本可能全错的组提供正样本对比。

**③ 一致性校正的损失**
被替换进来的轨迹在训练时按 **explain-style off-policy** 计算：由于它是在"改写提示"下生成、却要在"原始提示"下学习，我们用两种提示下对数概率之差做重要性校正（off-ratio = exp(logp_原始 − logp_改写)），使其等价于正确的策略梯度；其余在线轨迹照常走 GRPO。

**④ 失败难题回收**
训练过程中持续监测每道题的通过率，将"仍然偏难"的题目收入失败缓冲区。当缓冲区积累到阈值，插入若干**回收训练步**：对这些难题统一使用 summarize 提示做渐进式引导生成，再训练数轮；超过回收上限的题目不再重复处理，避免过拟合个别难题。

- **收集标准（准确率窗口）**：只收集通过率落在 **[10%, 50%)** 区间的题目——排除"全错（0%）无从下手"和"已学会一半（≥50%）"的两端，聚焦真正处于学习临界区的难题。
- **回收时的 summarize 比例**：回收步中每题的 **全部 8 条 rollout 均使用 summarize 提示**引导生成（区别于在线替换的 1/8），使用的prefix ratio 为固定的0.6，对难题做集中的强提示训练。

**⑤ 质量把关**
所有由 summarize 生成的改写轨迹都经过**轨迹过滤**：复述提示模板、引用草稿、或含明显重复噪声的输出被判为不合格，不会污染训练信号。

